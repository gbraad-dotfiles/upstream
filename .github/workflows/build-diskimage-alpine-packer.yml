name: Build and Push Alpine cloud diskimage

on:
  push:
    branches:
      - "main"
  schedule:
    - cron: "0 0 */2 * *"
  workflow_dispatch:


jobs:
  build-diskimage-alpine-packer:
    uses: ./.github/workflows/reusable-build-diskimage-packer.yml
    with:
      container_name: alpine-cloud
